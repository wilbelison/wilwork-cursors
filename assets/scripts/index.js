const variantsButtons = components.querySelectorAll("#component button");
const textAreaContainer = document.querySelector("#test-area");
const textAreaContent = textAreaContainer.querySelector(".content");

for(button of variantsButtons) {
   console.log(button);
}